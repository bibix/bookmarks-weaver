---
apply: always
---

Store design documentation in `design` directory.
Create scripts to execute in git-bash in `bin` directory. Before executing any command, create a script first in the `bin` directory and run it.
All commands are executed in git bash. Current folder is root of the project, so relative paths can be used.

Respect AGENTS.md file. Summarize any relevant claude or agent information in AGENTS.md files.