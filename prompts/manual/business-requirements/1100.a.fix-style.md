Here are the **precise instructions** for the code editing agent to update the "Bookmarks Weaver" UI implementation based on the latest image and the project requirements:

---

### ✅ General Styling Instructions

* **Framework**: Ensure TailwindCSS is integrated.
* **Design goal**: Modern, clean, and appealing layout with consistent spacing, rounded corners, and readable typography.
* **Color scheme**: Use a soft beige/light background with color-coded labels (blue, green, orange, violet) for URL parts and tags.

---

### 🔗 URL Input and Parsing Section

1. **URL Input Field**:

  * Use a wide, rounded `input` with Tailwind classes like `w-full p-3 rounded-md border border-gray-300`.

2. **Parsed URL Components**:

  * Split each part (scheme, domain, port, path, query key/value, fragment) into clearly separated tags.
  * Use color-coded pills:

    * `https`: `bg-blue-300 text-blue-800`
    * domain: `bg-green-200 text-green-800`
    * port: `bg-purple-200 text-purple-800`
    * path: `bg-orange-200 text-orange-800`
    * query key/value: listed separately (see below)
    * fragment: `bg-blue-200 text-blue-800`
  * Layout: Use a `flex flex-wrap gap-2` container for parsed fields.

---

### 🧩 Query Parameters

* **Separate query key/value rendering**:

  * Each key and each value must be rendered **individually** in its own label or div.
  * Tailwind example: `bg-gray-100 text-gray-800 px-2 py-1 rounded`.

---

### 🧠 Variable Definitions

* Section title: `Variables`
* Replace single-line lists and tables with:

  * **Multiline textareas** for lists (e.g., `<textarea class="w-full p-2 border rounded" rows="3"></textarea>`)
  * **Each column** in the table must be a **separate multiline textarea** (stacked or side-by-side in a grid).

---

### 🧾 Generated URLs

* Render generated URLs in a scrollable `<div>` or `<textarea>`:

  * Use monospace font.
  * Each URL on a new line.
  * Add padding and clear contrast between background and text.

---

### 📥 Download Button

* Style with Tailwind: `bg-teal-600 text-white px-6 py-2 rounded-md hover:bg-teal-700 transition`.

---

Let me know if you want the code updated directly, or if you'd like a specific framework structure or component layout.
