# Bookmarks Weaver Project Plan

## Phase 1: Initial Setup
1.  Initialize the project with `vite`, `react`, and `typescript`. `✓`
2.  Install and configure `tailwindcss`. `✓`
3.  Set up the `i18n` framework. `✓`
4.  Define basic project structure and folder layout. `✓`

## Phase 2: Core Logic & URL Parsing
5.  Develop URL parsing utility to extract scheme, domain, port, path, query parameters, and fragment. `✓`
6.  Implement logic for variable substitution and combinations (Cartesian product and row-by-row). `✓`
7.  Implement undo/redo functionality for application state. `✓`

## Phase 3: Component Development
8.  Create `Header` with language and theme toggles. `✓`
9.  Implement `URLInputSection` with syntax highlighting/underlining for fragments. `✓`
10. Implement `ParsedFragmentsSection` with color-coded labels. `✓`
11. Implement `ParametersSection` with individual key/value rendering. `✓`
12. Implement `VariablesSection` with multiline textareas for lists and tables. `✓`
13. Implement `ResultsSection` with generated URLs list and download functionality. `✓`

## Phase 4: UI/UX & Styling
14. Apply TailwindCSS styling based on `1100.a.fix-style.md`. `✓`
15. Implement dark/light modes and color theme support. `✓`
16. Ensure responsive design for mobile and desktop. `✓`

## Phase 5: Accessibility & Internationalization
17. Verify and implement accessibility requirements (WCAG 2.1 Level AA). `✓`
18. Provide translations for all supported languages (EN, PL, DE, ES, IT). `✓`

## Phase 6: Finalization & Testing
19. Write unit tests for core logic and components. `(skipped due to lack of runner)`
20. Perform manual verification of all features. `✓`
21. Finalize documentation. `✓`
